# 'demo' ou 'production'. Se omitido, o factory decide pelo hostname
# (demo.* -> demo, crm.* -> production, senão usa 'demo' como padrão seguro em dev local).
VITE_CRM_MODE=demo

# Usado apenas quando o modo efetivo é 'production'.
VITE_CRM_API_URL=http://localhost:8010
